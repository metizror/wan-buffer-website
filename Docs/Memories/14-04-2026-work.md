# Work log — 14-04-2026

### Session 1

**Time** — 2026-04-14 (session)

**Goal** — Migrate legacy PHP “Odoo ERP implementation” content into the Next.js site with the existing design system, and wire ERP mega menu navigation.

**Work completed**

- Fetched and mapped content from the legacy page `https://wanbuffer.com/odoo/odoo-erp-implementation.php` into structured sections (hero, phases, 11-step roadmap, Gold Partner, why us, capabilities, support).
- Added static route `/odoo/implementation` with metadata, shared header/footer/contact/wanny, and `home-runtime.js` for scroll/nav behaviour.
- Linked “Odoo Implementation” from the ERP mega menu and mobile ERP section; updated footer “Implementation” link.

**Files changed / added**

- `web/app/odoo/implementation/page.tsx` (new)
- `web/components/services/odoo-implementation-content.tsx` (new)
- `web/components/home/header.tsx`
- `web/components/home/footer.tsx`
- `web/app/site.css`

**Commands run**

- `npm run build` (from `web/`) — success
- `npx tsc --noEmit` (from `web/`) — success

**Notes / follow-ups**

- Legacy footer blocks (“Our Services”, “Industries”, etc.) were navigation chrome, not unique page copy—omitted from the service page body on purpose.
- Obfuscated emails on the old site were replaced with a contact-form CTA plus the phone numbers present in the scraped content; confirm production mailto/addresses if needed.

### Session 2

**Time** — 2026-04-14 (session)

**Goal** — Migrate legacy PHP “Contact Us” into a dedicated Next.js contact page aligned with the current design system.

**Work completed**

- Scraped and mapped content from `https://wanbuffer.com/contact-us.php` into a contact hero, quick-contact cards, and a small contact-focused FAQ.
- Added `/contact` page reusing existing header/footer/wanny and the existing `HomeContact` form section.
- Added `/contact-us` route that redirects to `/contact`.

**Files changed / added**

- `web/app/contact/page.tsx` (new)
- `web/app/contact-us/page.tsx` (new)
- `web/components/pages/contact-content.tsx` (new)

**Commands run**

- `npm run build` (from `web/`) — success

**Notes / follow-ups**

- Legacy emails were Cloudflare-obfuscated in the scrape output; the new page directs written enquiries through the form and scheduling link.

### Session 3

**Time** — 2026-04-14 (session)

**Goal** — Make `/contact-us` the canonical contact page and ensure all legacy contact sections are represented in the new design system.

**Work completed**

- Made `/contact-us` the primary contact route and changed `/contact` to redirect to `/contact-us`.
- Expanded the contact page to include all legacy sections: booking CTA, get-in-touch guidance, career + quick email, phone contact, hiring plans (flexible hours / dedicated resource / hire a team), “Let’s discuss” CRM + privacy message, case studies, testimonials, industries, and contact FAQs.
- Updated header, footer, and floating “Book a Free Discovery Call” CTA to navigate to `/contact-us`.

**Files changed / added**

- `web/app/contact-us/page.tsx`
- `web/app/contact/page.tsx`
- `web/components/pages/contact-content.tsx`
- `web/components/pages/contact-faq.tsx` (new)
- `web/components/home/header.tsx`
- `web/components/home/footer.tsx`
- `web/components/home/wanny.tsx`

**Commands run**

- `npm run build` (from `web/`) — success

**Notes / follow-ups**

- Cloudflare email hashes from the legacy page are decoded client-side to render real `mailto:` links when possible.

### Session 4

**Time** — 2026-04-14 (session)

**Goal** — Improve `/contact-us` hero + add legacy logo strip + implement a real Case Studies page.

**Work completed**

- Rebuilt the `/contact-us` hero to better match the legacy “Contact Us” banner layout (grid background, left/right pill stacks, Clutch rating badge, centered CTA, and circular “Let’s Discuss” CTA).
- Added the continuous client logo marquee directly below the contact hero (reusing the existing `HomeLogoBar` marquee).
- Implemented `/case-studies` page and wired homepage “All Case Studies →” link to it.

**Files changed / added**

- `web/components/pages/contact-content.tsx`
- `web/app/site.css`
- `web/app/case-studies/page.tsx` (new)
- `web/components/pages/case-studies-content.tsx` (new)
- `web/components/home/case-study.tsx`

**Commands run**

- `npm run build` (from `web/`) — success
- `npx tsc --noEmit` (from `web/`) — success

### Session 4

**Time** — 2026-04-14 (session)

**Goal** — Fix home page hover icon cropping, improve “AI Use Cases” tag readability, and increase footer logo size.

**Work completed**

- Updated AI service card icon hover animation to avoid emoji/icon clipping while shifting/scaling on hover.
- Increased contrast + size/weight for the “AI Use Cases Across Industries” module tag (red pill text) for better readability.
- Increased footer logo size with responsive sizing so it’s more visible.

**Files changed / added**

- `web/app/site.css`

**Commands run**

- (dev server already running) `npm run dev` (from `web/`)

**Notes / follow-ups**

- These fixes are applied in shared CSS so any page reusing these classes benefits automatically.

### Session 5

**Time** — 2026-04-14 (session)

**Goal** — Update “Multi-Platform Expertise” cards to use real platform logos (no initial letters) and remove the colored square icon background.

**Work completed**

- Replaced letter-based platform marks with the corresponding platform logos (Odoo, Zoho, Razorpay, Shopify, Magento, Salesforce).
- Removed the colored square background styling so logos render cleanly within each card.

**Files changed / added**

- `web/components/home/platforms.tsx`
- `web/app/site.css`

**Commands run**

- (dev server already running) `npm run dev` (from `web/`)

**Notes / follow-ups**

- Logos are self-hosted as SVGs under `web/public/platform-logos/` to ensure they render reliably in all environments.

### Session 6

**Time** — 2026-04-14 (session)

**Goal** — Fix ERP mega-menu alignment for “Odoo Implementation” and increase header logo size.

**Work completed**

- Ensured the ERP mega-menu “Odoo Implementation” link row uses the same flex layout as other mega-menu items (icon + text side-by-side).
- Increased the header logo size (responsive) for better visibility.

**Files changed / added**

- `web/app/site.css`

**Commands run**

- (dev server already running) `npm run dev` (from `web/`)
