# Work log — 20-05-2026

### Session 1

**Time** — 20-05-2026 (session)

**Goal** — Smoother FAQ accordion open/close transitions.

**Work completed**

- Replaced `max-height` FAQ accordion with CSS `grid-template-rows: 0fr` / `1fr` so height animates to real content (no 400px cap, smoother easing).
- Tweaked `.faq-arrow` transition so rotation does not fight `transition: all`.
- Added `prefers-reduced-motion` to disable accordion transition when requested.

**Files changed / added**

- `web/app/site.css`
- `Docs/Memories/20-05-2026-work.md` (new)

**Commands run**

- (none)

**Notes / follow-ups**

- Applies to all `.faq-section` blocks (home FAQ, contact FAQ, Odoo customization FAQ).

### Session 2

**Time** — 20-05-2026 (session)

**Goal** — Fix FAQ accordion so closed items actually hide answers (user report: all answers visible despite + icons).

**Work completed**

- Reverted FAQ panel animation from `grid-template-rows: 0fr` / `1fr` to `max-height: 0` → `max-height: 3000px` when `.open`, because `0fr` failed to collapse in the user’s environment (toggle state was correct; CSS was not hiding content).
- Set `window.toggleFaq = toggleFaq` in `home-runtime.js` so Next.js-loaded script always exposes the handler on `window`.

**Files changed / added**

- `web/app/site.css`
- `web/public/home-runtime.js`
- `Docs/Memories/20-05-2026-work.md`

**Commands run**

- (none)

**Notes / follow-ups**

- If any FAQ answer ever exceeds ~3000px height, raise the open `max-height` or switch to a measured-height approach in React.

### Session 3

**Time** — 20-05-2026 (session)

**Goal** — Match legacy PHP FAQ motion (slideUp/slideDown ~800ms, height to content) and reduce page “jump” from animating a huge fixed max-height.

**Work completed**

- Pulled behavior from live `wanbuffer.com` Odoo PHP page: jQuery `slideUp(800)` / `slideDown(800)` on `.faqq_text` next to `.faqq_box_que`.
- Replaced fixed `max-height: 3000px` transition with **measured** `max-height` (current `scrollHeight` → `0` / `0` → `scrollHeight`) in `toggleFaq`, **800ms** easing `cubic-bezier(0.25, 0.46, 0.45, 0.94)` (close to legacy ease).
- Added `initFaqAccordion` / `window.initFaqAccordion`, boot on load + double `requestAnimationFrame`, `filterFaq` calls init after category filter; `window.filterFaq` for consistency.
- Added `useFaqAccordionInit` hook so client navigations re-sync panel heights after React mounts FAQ DOM.
- CSS: default transition **0.8s**; `.faq-item.open .faq-a` uses `max-height: min(240vh, 3200px)` as pre-JS / fallback only.

**Files changed / added**

- `web/public/home-runtime.js`
- `web/app/site.css`
- `web/lib/use-faq-accordion-init.ts` (new)
- `web/components/home/faq.tsx`
- `web/components/pages/contact-faq.tsx`
- `web/components/services/odoo-customization-faq.tsx`
- `Docs/Memories/20-05-2026-work.md`

**Commands run**

- `curl.exe` to inspect live PHP HTML/inline scripts (reference only; temp copies removed).

**Notes / follow-ups**

- Very long FAQ copy still capped by fallback `min(240vh, 3200px)` until JS runs; if needed, raise cap or add `ResizeObserver` to refresh open panel height when fonts load.

### Session 4

**Time** — 20-05-2026 (session)

**Goal** — Remove redundant content on Odoo customization page.

**Work completed**

- Removed the **Customization roadmap** block (nine-stage chapter UI) because it largely duplicated the **Eight steps** methodology section above it.
- Dropped the unused **root-cause** `BENTO_DATA` row (`size: "feature"`) that never rendered in the grid.
- Removed **dead icon imports** tied to the removed sections.
- Aligned closer copy with the page: **“Eight-step methodology…”** instead of “11-stage roadmap”.

**Files changed / added**

- `web/components/services/odoo-customization-content.tsx`
- `Docs/Memories/20-05-2026-work.md`

**Commands run**

- (none)

**Notes / follow-ups**

- `#roadmap` was not linked from this file; other Odoo service pages still have their own roadmap sections if you want the same trim later.

### Session 5

**Time** — 20-05-2026 (session)

**Goal** — Remove the **Why Wan Buffer** section from the Odoo customization page.

**Work completed**

- Removed the full **`oi-bento-sec`** block (`id="why-us"`: eyebrow “Why Wan Buffer”, credentials strip, hero banner “Why Wan Buffer Services”, and bento card grid).
- Deleted **`BENTO_DATA`** and icon imports used only by that section.

**Files changed / added**

- `web/components/services/odoo-customization-content.tsx`
- `Docs/Memories/20-05-2026-work.md`

**Commands run**

- (none)

### Session 6

**Time** — 20-05-2026 (session)

**Goal** — Restore **Why Wan Buffer** bento section (user revert).

**Work completed**

- Re-added **`BENTO_DATA`**, related **icon imports**, and full **`oi-bento-sec`** / **`#why-us`** section as before Session 5.

**Files changed / added**

- `web/components/services/odoo-customization-content.tsx`
- `Docs/Memories/20-05-2026-work.md`

**Commands run**

- (none)

### Session 7

**Time** — 20-05-2026 (session)

**Goal** — Remove **Why Wan Buffer** bento section from `/odoo/migration`.

**Work completed**

- Removed **`oi-bento-sec`** / **`#why-us`** and **`BENTO_DATA`** from `odoo-migration-content.tsx`.
- Dropped unused icon imports (`BookOpenIcon`, `GraduationIcon`, `GridIcon`, `MilestoneIcon`, `MonitorIcon`, `SparklesIcon`, `TargetIcon`, `TrophyIcon`, `WalletIcon`).
- Updated migration content file header comment.

**Files changed / added**

- `web/components/services/odoo-migration-content.tsx`
- `Docs/Memories/20-05-2026-work.md`

**Commands run**

- (none)

### Session 8

**Time** — 20-05-2026 (session)

**Goal** — Remove **Choosing your partner** block from `/odoo/migration` closer section.

**Work completed**

- Removed **`oi-closer-hdr`** (eyebrow “Choosing your partner”, headline, insight stat, and bridge paragraph). Kept commitments grid and bottom CTA.

**Files changed / added**

- `web/components/services/odoo-migration-content.tsx`
- `Docs/Memories/20-05-2026-work.md`

**Commands run**

- (none)

### Session 9

**Time** — 20-05-2026 (session)

**Goal** — Remove **What we commit to · in writing** block from `/odoo/migration`.

**Work completed**

- Removed the four-card **`oi-closer-commits`** grid and its label. **`#capabilities`** now only contains the closing CTA row.

**Files changed / added**

- `web/components/services/odoo-migration-content.tsx`
- `Docs/Memories/20-05-2026-work.md`

**Commands run**

- (none)

### Session 10

**Time** — 20-05-2026 (session)

**Goal** — Remove **Odoo Gold Partner: what you gain** section from `/odoo/migration`.

**Work completed**

- Removed full **`oi-gp`** / **`#gold-partner`** block (header, spotlight, feature list, comparison table, CTA note).
- Deleted **`GOLD_FEATURES`** and **`COMPARE`** data; removed **`CpuIcon`**, **`CrossIcon`**, **`PhoneIcon`** imports (only used there).
- File header comment already reflected no Gold block after prior edit; left as “Closer · Clean support”.

**Files changed / added**

- `web/components/services/odoo-migration-content.tsx`
- `Docs/Memories/20-05-2026-work.md`

**Commands run**

- (none)

### Session 11

**Time** — 20-05-2026 (session)

**Goal** — Align `/odoo/odoo-erp-integration` with legacy `odoo-erp-integration.php` content only; drop extra Next-only sections.

**Work completed**

- Replaced `OdooIntegrationContent` with a PHP-faithful layout: banner, intro + three service paragraphs, “Why integration” + five boxes, two image+text rows, payment-gateway CTA band, Odoo Support strip.
- Added scoped **`oint-*`** styles in `site.css` (later replaced in Session 12).
- Updated route metadata (title, description, keywords, OpenGraph) to match the legacy page meta.
- Removed temporary reference file `web/_ref-integration-php.html`.

**Files changed / added**

- `web/components/services/odoo-integration-content.tsx`
- `web/app/site.css`
- `web/app/odoo/odoo-erp-integration/page.tsx`
- `web/_ref-integration-php.html` (deleted)
- `Docs/Memories/20-05-2026-work.md`

**Commands run**

- (none; integration images were already under `web/public/odoo/integration/`)

**Notes / follow-ups**

- Legacy Cloudflare-obfuscated emails on PHP were represented as `#contact` where no plain address exists in repo.

### Session 12

**Time** — 20-05-2026 (session)

**Goal** — Match `/odoo/odoo-erp-integration` look and feel to `/odoo/implementation` without adding extra sections.

**Work completed**

- Rebuilt integration page using shared **`oi-*`** patterns: `oi-hero-lux`, `oi-meth` + `oi-meth-intro`, `oi-rmap` + why grid, `oi-bento-sec` for two image rows, `oi-supp` for support (same card structure as implementation). (Payment-gateway `cta-banner` removed in Session 13.)
- Replaced bespoke **`oint-*`** CSS with compact **`oi-int-*`** helpers (split image styling, service stack, why grid cells, CTA spacing).
- Kept integration-only copy (no stats, methodology, roadmap, gold, bento marketing, or closer blocks).

**Files changed / added**

- `web/components/services/odoo-integration-content.tsx`
- `web/app/site.css`
- `Docs/Memories/20-05-2026-work.md`

**Commands run**

- (none)

**Notes / follow-ups**

- Hero secondary link scrolls to `#intro`.

### Session 13

**Time** — 20-05-2026 (session)

**Goal** — Remove duplicate CTA: “Odoo Integration With Different Payment Gateway Integration” from `/odoo/odoo-erp-integration` (support/contact already covered elsewhere).

**Work completed**

- Removed the **`cta-banner`** payment-gateway block from `OdooIntegrationContent`.
- Dropped unused **`oi-int-pg-accent`** / **`oi-int-cta`** CSS rules.

**Files changed / added**

- `web/components/services/odoo-integration-content.tsx`
- `web/app/site.css`
- `Docs/Memories/20-05-2026-work.md`

**Commands run**

- (none)

### Session 14

**Time** — 20-05-2026 (session)

**Goal** — Align `/odoo/training` with `wanbuffer.com/odoo/odoo-training.php` content; remove extra Next-only sections.

**Work completed**

- Replaced `OdooTrainingContent` with PHP-faithful structure: hero (banner copy), three pillar cards (`hp_service` images), functional / technical / corporate sections with `odoo_training_*.webp` splits and box grids, six technical package cards with box icons, `oi-supp` support strip.
- Removed stats, methodology, roadmap, gold partner, bento, and closer blocks.
- Downloaded assets to `web/public/odoo/training/` (main images, box icons, pillar icons).
- Updated `page.tsx` metadata to match PHP title/description (trimmed keyword list).
- Added `oi-train-*` styles in `site.css`; removed temp `web/_ref-training-php.html`.

**Files changed / added**

- `web/components/services/odoo-training-content.tsx`
- `web/app/site.css`
- `web/app/odoo/training/page.tsx`
- `web/public/odoo/training/*.webp` (new)
- `Docs/Memories/20-05-2026-work.md`

**Commands run**

- `curl.exe` to fetch PHP HTML/meta and training images.

**Notes / follow-ups**

- Knowledge Requirement copy uses “aspects” (corrected from PHP typo “aspaects”). Package card images use decorative `alt=""` (icons); pillar images keep descriptive alts.

### Session 15

**Time** — 20-05-2026 (session)

**Goal** — Fix training “Functional” style blocks: avoid cramped five-column cards and narrow-column bullet lists.

**Work completed**

- Extended `TrainingSplit` with optional `below` so `TrainingHighlightList` renders full-width under the image + copy row (wrapper `.oi-train-split-wrap`).
- Moved all highlight lists from the right column into `below` for every split that used them.
- CSS: split wrap column gap, zero extra top margin on highlights inside wrap, adjacent `.oi-train-split-wrap` spacing, section-title → first split margin (same as prior `.oi-meth-intro` rules).

**Files changed / added**

- `web/components/services/odoo-training-content.tsx`
- `web/app/site.css`
- `Docs/Memories/20-05-2026-work.md`

**Commands run**

- `npx tsc --noEmit` (in `web/`)

**Notes / follow-ups**

- If the old five-box layout still appears, hard-refresh or restart dev (stale `.next`).

### Session 16

**Time** — 20-05-2026 (session)

**Goal** — Alternate image/copy layout across Odoo Functional, Technical, and Corporate training blocks.

**Work completed**

- Added `mediaPosition` (`"start"` | `"end"`) to `TrainingSplit`; `"end"` adds `oi-train-split--media-end` (CSS `order` swap from 761px up; mobile resets to image-first DOM order).
- Functional + intro splits: default (`start`, image left). Technical section splits: `end` (image right). Corporate: default (`start`).

**Files changed / added**

- `web/components/services/odoo-training-content.tsx`
- `web/app/site.css`
- `Docs/Memories/20-05-2026-work.md`

**Commands run**

- `npx tsc --noEmit` (in `web/`)

**Notes / follow-ups**

- For a stronger zig-zag *within* one section (row-by-row), pass `mediaPosition` per split instead of per-section defaults.

### Session 17

**Time** — 20-05-2026 (session)

**Goal** — Training page: alternate image left / right on each consecutive content split (not only per Functional / Technical / Corporate band).

**Work completed**

- Set explicit `mediaPosition` on every `TrainingSplit` in page order: start, end, start, end, … from functional intro through corporate target audience.
- Adjusted Technical “How to enroll” from `end` to `start` so the chain continues correctly after Functional target (start) → Technical packages (end) → enroll (start) → significance (end) → Corporate packages (start) → …

**Files changed / added**

- `web/components/services/odoo-training-content.tsx`
- `Docs/Memories/20-05-2026-work.md`

**Commands run**

- `npx tsc --noEmit` (in `web/`)

### Session 18

**Time** — 20-05-2026 (session)

**Goal** — Training highlights: keep checklist beside the image (same split / text column), not a full-width extra row below.

**Work completed**

- Removed `TrainingSplit` `below` prop; moved every `TrainingHighlightList` back under the intro paragraph inside `oi-train-split-copy` so it sits in the two-column split next to the image.
- CSS: dropped obsolete wrap gap / highlights margin hack; added `.oi-train-split-copy` highlight spacing and slightly tighter row padding.

**Files changed / added**

- `web/components/services/odoo-training-content.tsx`
- `web/app/site.css`
- `Docs/Memories/20-05-2026-work.md`

**Commands run**

- `npx tsc --noEmit` (in `web/`)

### Session 19

**Time** — 20-05-2026 (session)

**Goal** — Align Next hosting page with `wanbuffer.com/odoo/odoo-hosting.php` at **`/odoo/hosting`** (canonical).

**Work completed**

- Replaced implementation-style `OdooHostingContent` with PHP-shaped sections: lux hero, intro band, image splits, prose blocks, support strip.
- **`/odoo/hosting`**: full `page.tsx` lives under `web/app/odoo/hosting/`; `alternates.canonical` → `https://wanbuffer.com/odoo/hosting`.
- Removed standalone `web/app/hosting/page.tsx`; **`next.config.ts`** permanent redirect `/hosting` → `/odoo/hosting` for old links.
- Nav/footer → `/odoo/hosting`; `oi-host-*` CSS comment updated.

**Files changed / added**

- `web/components/services/odoo-hosting-content.tsx`
- `web/app/odoo/hosting/page.tsx`
- `web/app/site.css`
- `web/next.config.ts`
- `web/components/home/header.tsx`
- `web/components/home/footer.tsx`
- `web/public/odoo/hosting/odoo_migration_*.webp`
- `Docs/Memories/20-05-2026-work.md`

**Commands run**

- `curl.exe` (reference), `npx tsc --noEmit` (in `web/`)

**Notes / follow-ups**

- PHP “Contact us” list item → CTA line + `#contact`. Legacy `/hosting` URL redirects to `/odoo/hosting`.

### Session 20

**Time** — 20-05-2026 (session)

**Goal** — New page from `wanbuffer.com/odoo/odoo-consultant.php` at **`/odoo/odoo-consultant`**; no extra marketing sections.

**Work completed**

- Added `OdooConsultantContent`: hero (PHP h1 title only), intro band (PHP h3 + paragraph + Chat now), four image splits with PHP copy/lists, “Why do you require…” prose block, “Value of Odoo Consultants” split, Odoo Support (three cards; no commit strip).
- `web/app/odoo/odoo-consultant/page.tsx` with PHP-aligned metadata/canonical.
- Images `odoo_migration_1..4.webp` in `web/public/odoo/consultant/`.
- Footer “Odoo Consultancy” → `/odoo/odoo-consultant`; `.oi-consult-intro-h` CSS.

**Files changed / added**

- `web/components/services/odoo-consultant-content.tsx` (new)
- `web/app/odoo/odoo-consultant/page.tsx` (new)
- `web/app/site.css`
- `web/components/home/footer.tsx`
- `web/public/odoo/consultant/*.webp` (new)
- `Docs/Memories/20-05-2026-work.md`

**Commands run**

- `curl.exe` (PHP HTML + images), `npx tsc --noEmit` (in `web/`)

### Session 21

**Time** — 20-05-2026 (session)

**Goal** — Fix broken Events page UI (`/event`) and redesign to match site look & feel.

**Work completed**

- Root cause: `oi-evt-*` and `oi-train-support` classes had no CSS — layout was unstyled.
- Added full Events styles in `site.css` (search panel, 3-col cards, promo banner, responsive breakpoints).
- Redesigned `events-listing.tsx`: category tags, clear filters, `fill` images with aspect ratio, site `eyebrow`/`sec-h` headings.
- Replaced broken support block with `oi-supp` section (three cards) in `events-content.tsx`.
- Header + mobile nav: **Events** → `/event`.

**Files changed / added**

- `web/app/site.css`
- `web/components/pages/events-listing.tsx`
- `web/components/pages/events-content.tsx`
- `web/components/home/header.tsx`
- `Docs/Memories/20-05-2026-work.md`

**Commands run**

- `npm run build` (in `web/`, success)

### Session 22

**Time** — 20-05-2026 (session)

**Goal** — Redesign case study listing cards to feel more realistic/editorial; keep all copy unchanged.

**Work completed**

- Restructured `case-study-listing.tsx`: single full-card `Link`, photo on top with gradient overlay and client badge, body below with title/excerpt/metrics/CTA (same text from data).
- Updated `oi-cs-*` card CSS: 2-column results grid, 16:10 hero image, depth/shadow/hover lift and image zoom, 2×2 metric strip inside card.
- Added `aria-label` on card links for accessibility.

**Files changed / added**

- `web/components/pages/case-study-listing.tsx`
- `web/app/site.css`
- `Docs/Memories/20-05-2026-work.md`

**Commands run**

- `npm run build` (in `web/`, success)

**Notes / follow-ups**

- Review at `http://localhost:3002/case-study`; say if you want tighter spacing or single-column on tablet.

### Session 23

**Time** — 20-05-2026 (session)

**Goal** — Fix Distribution industry filter: show 2 correct case studies (match live WordPress taxonomy).

**Work completed**

- Compared `web/_ref-cs-api.json` `class_list` to local `industries` / `regions` / `technologies`.
- **Distribution (2):** `odoo-steel-solution`, `odoo-integration-ecommerce-retailer` (removed incorrect `distribution` from `odoo-erp-implementation` Magento study).
- Aligned all six studies’ filter tags with WP (textile + canada; Magento → ecommerce/hospitality/textile; Omahat → magento only; etc.).

**Files changed / added**

- `web/lib/case-study-data.ts`
- `Docs/Memories/20-05-2026-work.md`

**Commands run**

- `npm run build` (in `web/`)

### Session 24

**Time** — 20-05-2026 (session)

**Goal** — Match contact/support cards on case study detail pages to listing (and event detail) structure.

**Work completed**

- `case-study-detail-content.tsx`: added `oi-supp-label` and `oi-supp-body-v2` on all three cards; third card aligned with listing (`Odoo implementation` / Learn more).

**Files changed / added**

- `web/components/pages/case-study-detail-content.tsx`
- `Docs/Memories/20-05-2026-work.md`