## 09-04-2026 — Work Log

- **Project**: WAN-BUFFER-WEBSITE
- **Timezone**: (fill)

---

### Session 1

- **Time**: (fill)
- **Goal**: Memory system setup (daily + session-wise logs)

#### Work completed

- Created `Docs/Memories/` and added daily log convention.
- Added `Docs/Memories/README.md` describing how we’ll maintain day-wise/session-wise logs.
- Created today’s log file `Docs/Memories/09-04-2026-work.md`.

#### Files changed / added

- `Docs/Memories/README.md` (added)
- `Docs/Memories/09-04-2026-work.md` (added)

#### Notes / follow-ups

- After every future session today, we will append a new `### Session N` block here capturing: features implemented, changes made, and files touched.

---

### Session 2

- **Time**: (fill)
- **Source**: [Next.js homepage + popup](363fed4c-b505-4a10-8e4b-f4a68314982a)
- **Goal**: Create Next.js website homepage from `files/wanbuffer-v11.html` and add the reference lead/banner popup behavior.

#### Work completed

- Built a **Next.js app under `web/`** (outside `files/`) and implemented the **homepage as a faithful port** of `files/wanbuffer-v11.html` (UI/UX, colors, fonts, layout, interactions).
- Extracted and wired the homepage as:
  - HTML body markup stored separately and rendered via `dangerouslySetInnerHTML`.
  - Runtime interactivity loaded after hydration via a client script.
- Implemented and refined the **lead/banner popup** to match the reference behavior:
  - **Scroll-triggered open** (after the user scrolls into the 2nd–3rd major band; triggers when the **AI strip** section enters view).
  - **Dismiss/submit persistence** using `localStorage` key `wanbuffer_lead_popup_dismissed_v1` (prevents re-opening on later visits until storage is cleared).
  - **Removed** old timer/progress bar behavior; improved **mobile layout** so the dark panel stacks above the form.
  - Form validation for **name, company, email, primary interest**.
- Styling system confirmed as **custom CSS** (no Tailwind):
  - Global CSS import via `web/app/globals.css` → `web/app/wanbuffer.css`.
  - Fonts via `next/font/google` and CSS variables mapped to `--font-*`.

#### Files changed / added

- `web/app/wanbuffer.css` (extracted full stylesheet; later popup styling updates)
- `web/app/globals.css` (imports `wanbuffer.css`)
- `web/app/layout.tsx` (metadata + font variables)
- `web/app/page.tsx` (renders `home-body.html` + loads `home-runtime.js`)
- `web/lib/home-body.html` (homepage markup; popup markup updates; added `id="case-studies"` anchor)
- `web/public/home-runtime.js` (homepage runtime behaviors; popup scroll observer; persistence helpers)

#### Commands run

- `cd web`
- `npm run dev`
- `npm run build`

#### Notes / follow-ups

- Tailwind can be added later (if desired) by mapping existing design tokens (colors/fonts/radii/etc.) into `tailwind.config.*` while keeping complex pieces in CSS during migration; current implementation is custom CSS-only.

---

### Session 3

- **Time**: (fill)
- **Goal**: Take a project backup (exclude `node_modules` + build outputs)

#### Work completed

- Created a full project backup (including `Docs/`) while excluding large/generated directories (`node_modules`, `.next`, `dist`, `build`, `out`, `.turbo`, `.vercel`, `.cache`, `coverage`).

#### Commands run

- `scripts/take-backup.ps1 -Title "converted-HTML-into-NEXTJS-2"`

#### Backup output

- **Destination root**: `D:\Rudra\MSPL-044\BACKUPS\wan-buffer-website-backup`
- **Created folder**: `09-04-2026-backup-converted-HTML-into-NEXTJS-2`
- **Full path**: `D:\Rudra\MSPL-044\BACKUPS\wan-buffer-website-backup\09-04-2026-backup-converted-HTML-into-NEXTJS-2`

